{
  "name": "doge-web-components",
  "version": "1.0.0",
  "description": "Reusable web components for Dogecoin",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "dev": "web-dev-server --root-dir dist --open / --port 8080 --node-resolve --watch",
    "test": "web-test-runner",
    "build": "./build.sh",
    "build:watch": "nodemon --watch src -e '*' --exec './build.sh'",
    "prepare": "husky"
  },
  "devDependencies": {
    "@open-wc/testing": "2.5.33",
    "@web/dev-server": "0.2.1",
    "@web/mocks": "^1.1.1",
    "@web/test-runner": "0.16.0",
    "@web/test-runner-playwright": "^0.11.0",
    "husky": "^9.0.11",
    "nodemon": "^3.1.0"
  }
}
